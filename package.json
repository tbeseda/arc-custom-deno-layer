{
  "name": "deno-runtime",
  "version": "0.0.0",
  "description": "Arc + custom Deno layer",
  "scripts": {
    "start": "npx arc sandbox"
  },
  "devDependencies": {
    "@architect/architect": "10.3.3"
  }
}
